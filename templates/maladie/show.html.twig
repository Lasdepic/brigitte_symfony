{% extends 'base.html.twig' %}

{% block title %}Maladie{% endblock %}

{% block body %}
    <h1>Maladie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maladie.id }}</td>
            </tr>
            <tr>
                <th>Nom_maladie</th>
                <td>{{ maladie.nomMaladie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_maladie_index') }}">back to list</a>

    <a href="{{ path('app_maladie_edit', {'id': maladie.id}) }}">edit</a>

    {{ include('maladie/_delete_form.html.twig') }}
{% endblock %}
