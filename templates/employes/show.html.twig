{% extends 'base.html.twig' %}

{% block title %}Employes{% endblock %}

{% block body %}
    <h1>Employes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Nom_employe</th>
                <td>{{ employe.nomEmploye }}</td>
            </tr>
            <tr>
                <th>Prenom_employe</th>
                <td>{{ employe.prenomEmploye }}</td>
            </tr>
            <tr>
                <th>Num_employe</th>
                <td>{{ employe.numEmploye }}</td>
            </tr>
            <tr>
                <th>Sexe_employe</th>
                <td>{{ employe.sexeEmploye }}</td>
            </tr>
            <tr>
                <th>Ville_employe</th>
                <td>{{ employe.villeEmploye }}</td>
            </tr>
            <tr>
                <th>Type_poste</th>
                <td>{{ employe.typePoste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employes_index') }}">back to list</a>

    <a href="{{ path('app_employes_edit', {'id': employe.id}) }}">edit</a>

    {{ include('employes/_delete_form.html.twig') }}
{% endblock %}
