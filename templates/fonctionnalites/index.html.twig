{% extends 'base.html.twig' %}

{% block title %}Fonctionnalites index{% endblock %}

{% block body %}
    <h1>Fonctionnalites index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Num_fonctionnalite</th>
                <th>Type_fonctionnalite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fonctionnalite in fonctionnalites %}
            <tr>
                <td>{{ fonctionnalite.id }}</td>
                <td>{{ fonctionnalite.numFonctionnalite }}</td>
                <td>{{ fonctionnalite.typeFonctionnalite }}</td>
                <td>
                    <a href="{{ path('app_fonctionnalites_show', {'id': fonctionnalite.id}) }}">show</a>
                    <a href="{{ path('app_fonctionnalites_edit', {'id': fonctionnalite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fonctionnalites_new') }}">Create new</a>
{% endblock %}
