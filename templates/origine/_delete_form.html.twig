<form method="post" action="{{ path('app_origine_delete', {'id': origine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ origine.id) }}">
    <button class="btn">Delete</button>
</form>
