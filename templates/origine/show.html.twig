{% extends 'base.html.twig' %}

{% block title %}Origine{% endblock %}

{% block body %}
    <h1>Origine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ origine.id }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ origine.Pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_origine_index') }}">back to list</a>

    <a href="{{ path('app_origine_edit', {'id': origine.id}) }}">edit</a>

    {{ include('origine/_delete_form.html.twig') }}
{% endblock %}
